;;; Copyright (c) 2012 by Álvaro Castro-Castilla, All Rights Reserved.
;;; Licensed under the GPLv3 license, see LICENSE file for full description.

;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;;; Mathematical sequences and series
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;

;-------------------------------------------------------------------------------
; Eager procedures
;-------------------------------------------------------------------------------



;-------------------------------------------------------------------------------
; Lazy procedures
;-------------------------------------------------------------------------------

;;; Fibonacci sequence

(define (fibonacci)
  (error "Not implemented"))
;; long int fib(unsigned long int n) {
;; return lround((pow(0.5 + 0.5 * sqrt(5.0), n) - 
;;                                              pow(0.5 - 0.5 * sqrt(5.0), n)) / 
;;               sqrt(5.0));
;; }

;;; Hofstadter Q sequence

(define (hofstadter-q)
  (error "Not implemented"))

;;; Formal power series

(define (power-series)
  (error "Not implemented"))

;;; Non-squares sequence

(define (non-squares)
  (error "Not implemented"))

;;; Formal difference sequence

(define (forward-differences)
  (error "Not implemented"))

;;; Juggler's sequence

(define (jugglers-sequence)
  (error "Not implemented"))

;;; Hailstone sequence

(define (hailstone-sequence)
  (error "Not implemented"))

;;; Hamming numbers

(define (hamming-numbers)
  (error "Not implemented"))

;;; Harmonic numbers

(define (harmonic-numbers)
  (error "Not implemented"))

;;; Roots of unity are the complex numbers that equals 1 when raised to some integer power n

(define (roots-of-unity)
  (error "Not implemented"))
